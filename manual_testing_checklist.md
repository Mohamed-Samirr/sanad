# قائمة اختبار تطبيق سند (Manual Testing Checklist)

إليك القائمة الشاملة للأشياء التي يجب عليك اختبارها وتجربتها يدوياً عند تشغيل التطبيق للتأكد من أن جميع الميزات التي تم بناؤها تعمل بكفاءة.

## 1. استقرار تخزين البيانات (Hive Refactoring)
قمنا بتعديل طريقة عمل قاعدة البيانات لتكون متزامنة وسريعة لتجنب أخطاء التعليق (Race Conditions):
- [ ] **إضافة وتعديل العادات:** جرب إضافة عادة جديدة، وتعديلها، وحذفها. تأكد أن كل شيء يتم حفظه فوراً دون تأخير أو انهيار للتطبيق.
- [ ] **تسجيل الانتكاسات (Urges):** قم بتسجيل رغبة أو انتكاسة جديدة من قسم التعافي وتأكد من حفظها بنجاح في السجل.

## 2. ميزة رؤى التعافي (Recovery Insights - Phase 2)
- [ ] **الوصول للشاشة:** اذهب إلى شاشة الإحصائيات (Insights) من الشريط السفلي (Bottom Navigation).
- [ ] **الرسوم البيانية:** تأكد من ظهور الرسوم البيانية (مثل رسم أوقات الانتكاسة وتتبع المثيرات Triggers).
- [ ] **التحديث الفوري:** قم بتسجيل انزلاقة (Urge) جديدة، ثم عد إلى شاشة الإحصائيات ولاحظ إذا كانت الأرقام والرسومات البيانية تتحدث تلقائياً لتعكس البيانات الجديدة.

## 3. إعدادات الخصوصية والبيانات (Privacy & Settings - Phase 3)
اذهب إلى صفحة الإعدادات وجرب الخيارات الموجودة في قسم **Privacy & Data**:
- [ ] **تصدير البيانات (Export Data):** اضغط على "تصدير" وتأكد أن التطبيق يفتح لك نافذة المشاركة الخاصة بالنظام (Share Sheet) لتصدير ملفات قواعد البيانات (`.hive`) الخاصة بك.
- [ ] **استيراد البيانات (Import Data):** اضغط على "استيراد" وجرب اختيار ملفات الـ Hive للتأكد من أن منتقي الملفات (File Picker) يفتح بشكل سليم، وأن البيانات يتم تحميلها.
- [ ] **مسح جميع البيانات (Wipe Data):** اضغط على هذا الخيار، وتأكد من ظهور رسالة التأكيد المزدوجة التحذيرية. إذا وافقت، تأكد أن التطبيق يمسح كل العادات والإحصائيات الخاصة بك ويعود لحالته الأصلية تماماً.
- [ ] **قفل التطبيق (App Lock):** 
  > [!NOTE]
  > قمنا بتجهيز البنية التحتية بالكامل لقفل التطبيق (Cubit & Services) ووضعنا زر البصمة في شاشة القفل، ولكن إعداد رقم الـ PIN يحتاج مستقبلاً إلى تصميم "شاشة لوحة أرقام" (PIN Pad) مخصصة (توجد تعليقات `TODO` في الكود). يمكنك تجربة البصمة إذا كان المحاكي / الهاتف يدعمها.

## 4. الاختبار العام (General Smoke Test)
- [ ] **تشغيل التطبيق من الصفر (Cold Start):** أغلق التطبيق من الخلفية تماماً وافتحه من جديد وتأكد أنه يفتح بشكل سليم بدون أن يعلق في شاشة التحميل.
- [ ] **التنقل السريع:** تنقل بسرعة بين التبويبات السفلية (العادات، التعافي، الإحصائيات، الإعدادات) وتأكد من سلاسة الواجهة وعدم وجود أي شاشات حمراء (Exceptions).
